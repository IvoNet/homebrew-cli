#!/usr/bin/env bash
IFS=$(echo -en "\n\b")
docker run \
   --cpuset-cpus="1-2" \
   --rm \
   -v "$(pwd):/input:ro" \
   -v "$(pwd):/output:rw" \
   --entrypoint "m4b" \
   ivonet/mediatools:latest \
   "$@"
